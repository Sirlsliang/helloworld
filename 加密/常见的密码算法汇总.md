## 常见的密码算法汇总
> 根据密钥类型不同将现代密码技术分为对称加密算法和非对称加密算法

#### 对称加密
- 系统加密和解密采用同一把秘密钥匙，而且通信双方都必须获得这把钥匙，并保持钥匙的秘密。
- 常见的对称加密算法：
  - DES(Data Encryption Standard):数据加密标准，速度较快，适用于加密大量数据的场合。使用56位的密钥过
短(现在已经不是安全的加密算法)
  - 3DES(Triple DES)：基于DES，对一块数据用三个不同的密钥进行三次加密，强度更高
  - AES(Advanced Encryption Standard):高级加密标准，速度快，安全级别高。采用区块加密标准，对称加密中
最流行的算法之一。

#### 非对称加密
- 需要一个公钥和一个私钥，两个密钥数学相关，利用一个密钥加密的信息，只能使用另一个密钥才能解密。
- 非对称加密算法
  - RSA: RSA公司发明，支持变长的密钥的公共密钥算法，需要加密的文件块长度也是可变的。
  - DSA(Digital Signature Algorithm):数字签名算法，是一种标准的DSS(数字签名标准)
  - ECC(Elliptic Curves Cryptography): 椭圆曲线密码编码学。

#### 安全散列算法
  - 将任意大小的数据映射到一个较小的、固定长度的唯一值，加密性强的散列一定是不可逆的，这就意味着通过
散列值，无法推出任何部分的原始信息。任何输入信息的变化，哪怕仅一位，都将导致散列结果的明显变化(雪崩
效应)。
  - 不可逆的、放冲突，并有明显的雪崩效应
  - 安全散列算法
    - SHA-1: 基于MD4算法,
    - MD5:

#### 编码
  - Base64编码：将三个8位字节(3\*8=24)转化成为4个6位的字节(4\*6=24)，之后在6位的前面补两个0，形成8位
一个字节的形式。如果剩下的字符不足3个字节，则用0填充，输出字符使用"=",因此编码后输出的文本后面可能
会有1个或者2个"="
  
